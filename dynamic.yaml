services:
    lb@app:
        loadBalancer:
            servers:
                - url: 'http://localhost:1111/'
                - url: 'http://localhost:2222/'
                - url: 'http://localhost:3333/'

http:
    routers:
        lb@app:
            rule: Path("/lbapp")
            service: lb@app
